"use client";

import Navbar from "@/Components/Navbar";
import { useAppContext } from "@/Context/AppContext";
const page = () => {
  return (
    <div>
      <Navbar relative />
      Admin Page
    </div>
  );
};

export default page;
